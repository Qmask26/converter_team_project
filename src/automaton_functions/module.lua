minimization = require("src/automaton_functions/minimization")
determinization = require("src/automaton_functions/determinization")
reverse = require("src/automaton_functions/reverse")

Module = {
    Minimize = minimization,
    Determinize = determinization,
    Reverse = reverse
}

return Module